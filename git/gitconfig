# vim: ts=4 sw=4 sts=0

[user]
    name = Andrew Thompson
    email = andrew@tobarja.com

[core]
    editor = vi

[alias]

[color]
    ui = auto

[merge]
    ff = only

[push]
    default = simple
    #simple is not supported by older git version, use upstream instead

[url "git@github.com:"]
	insteadOf = https://github.com/
